import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

public class Pepperoni extends Actor
{
    public Pepperoni() {
        GreenfootImage image = getImage();
        image.scale(image.getWidth() - 80, image.getHeight() - 60);
        setImage(image);
    }
    
    public void act() 
    {
        
    }    
}
